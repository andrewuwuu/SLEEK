<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".data.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sleek"
        tools:targetApi="31">
        <activity
            android:name=".ui.logout.LogoutActivity"
            android:exported="false" />
        <activity
            android:name=".ui.food.FoodListActivity"
            android:theme="@style/Theme.Sleek.NoActionBar"
            android:exported="false"
            android:parentActivityName=".ui.healthresult.ResultHealthActivity" />
        <activity
            android:name=".ui.healthresult.ResultHealthActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.DataUserActivity"
            android:theme="@style/Theme.Sleek.NoActionBar" />
        <activity
            android:name=".ui.main.DataUserActivity"
            android:exported="false" /> <!-- Menggunakan tema utama MaterialComponents -->
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.Sleek.Login" /> <!-- Menggunakan tema yang sama dengan login -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Sleek.Login" />
    </application>

</manifest>